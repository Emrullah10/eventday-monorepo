{
  "name": "everyday-mono-repo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "EventDay monorepo: identity/event/booking/aggregator services + web gateway + web app",
  "workspaces": [
    "packages/modules/*",
    "core/*",
    "services/*",
    "everyday-web-app"
  ],
  "scripts": {
    "build:schema": "node scripts/build-schema.js",
    "lint": "echo \"(no root linter configured yet)\"",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "ISC"
}
